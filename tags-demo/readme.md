# Terraform Object and Tagging

- use object variable to encapsulate tag values
- can be used as object prototype
- null_resource is used in this demo just to trigger output
- use map type for generic lookup table
